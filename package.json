{
  "scripts": {
    "api": "python -m uvicorn api.main:app --reload --host 127.0.0.1 --port 8001",
    "stream": "node streaming-server.js"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "socket.io": "^4.8.3"
  }
}
